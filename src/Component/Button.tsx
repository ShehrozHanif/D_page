const Button = ({name}:{name:string}) => {
    return (
      <div>
        <button className='bg-purple-700 py-3 px-8 text-white font-semibold'>{name}</button>
      </div>
    )
  }
  
  export default Button
  